# Contributing to Risk Calculator

Спасибо за интерес к улучшению Risk Calculator! Мы приветствуем вклад от сообщества.

## Как внести вклад

### Сообщение об ошибках

Если вы нашли баг, пожалуйста:
1. Проверьте, не был ли он уже сообщен в Issues
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Версия браузера и ОС

### Предложение новых функций

Для предложения новых функций:
1. Создайте Issue с тегом "enhancement"
2. Опишите проблему, которую решает функция
3. Предложите возможную реализацию
4. Обсудите с сообществом

### Pull Requests

1. Fork репозитория
2. Создайте ветку для вашей функции:
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. Внесите изменения
4. Убедитесь, что код соответствует стандартам:
   ```bash
   npm run build  # Проверка на ошибки TypeScript
   npm test       # Запуск тестов
   ```
5. Commit с понятным сообщением:
   ```bash
   git commit -m "feat: add amazing feature"
   ```
6. Push в вашу ветку:
   ```bash
   git push origin feature/amazing-feature
   ```
7. Создайте Pull Request

## Стандарты кода

### Стиль коммитов

Мы используем Conventional Commits:
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

### Стиль кода

- Используйте TypeScript для всего нового кода
- Следуйте существующему стилю кода
- Добавляйте JSDoc комментарии для публичных API
- Используйте осмысленные имена переменных и функций

### Тестирование

- Пишите unit тесты для новых функций
- Убедитесь, что все тесты проходят
- Стремитесь к покрытию кода > 80%

### Компоненты Vue

- Используйте Composition API с `<script setup>`
- Разделяйте логику на composables при необходимости
- Используйте TypeScript для props и emits
- Следуйте принципу единственной ответственности

## Структура проекта

```
src/
├── components/      # Vue компоненты
├── stores/         # Pinia stores
├── types/          # TypeScript типы
├── composables/    # Переиспользуемая логика (будущее)
└── utils/          # Утилиты (будущее)
```

## Приоритеты развития

### Высокий приоритет
- [ ] Персистентность данных (LocalStorage)
- [ ] Экспорт/импорт сценариев
- [ ] Мобильная адаптивность
- [ ] Темная/светлая тема

### Средний приоритет
- [ ] Учет комиссий
- [ ] Расчеты с плечом
- [ ] История расчетов
- [ ] Множественные позиции

### Низкий приоритет
- [ ] API интеграция для цен
- [ ] Графики и визуализация
- [ ] PWA поддержка
- [ ] i18n (интернационализация)

## Вопросы?

Если у вас есть вопросы, создайте Discussion или напишите в Issues.

Спасибо за вклад в Risk Calculator! 🚀
